# Knapsack WASM

## Cómo ejecutar la página localmente

Inicia un servidor HTTP simple en la carpeta del proyecto:

```bash
python3 -m http.server 8000
```

Luego abrir en tu navegador:

```text
http://localhost:8000/index.html
```
